.sidebar
  .sidebar__top 
    .sidebar__top__user
      = current_user.name
    .sidebar__top__icon
      = link_to new_group_path, class: "icon__edit" do
        %i.fa.fa-edit
      = link_to edit_user_path(current_user), class: "icon__config" do
        %i.fa.fa-cog 

  .sidebar__groups
    - current_user.groups.each do |group|
      .sidebar__groups__group
        = link_to "#", class: "sidebar__groups__group" do
          .sidebar__groups__group__name
            = group.name
          .sidebar__groups__group__message
            まだメッセージはありません